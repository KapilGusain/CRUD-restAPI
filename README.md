This is my backend work in which I built an Express server and connected it to MongoDB. I built a student registration and management site in which UI is
built using ejs and Express js is used for server. 
It has all the CRUD(Create, Read, Update and Delete) operations performed on the database which helped me understand all basics and advanced problems when 
performing operations on the database.
